'use client';

/**
 * Ré-export de compatibilité — le hook vit désormais dans `gestures/`.
 * Chemin historique conservé pour ne rien casser (mission gestes, Phase 1).
 */
export { useSwipe } from './gestures/useSwipe';
