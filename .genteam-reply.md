Chantier H0-H8 terminé en deltas sur `chantier/etape2-hub-unique`, tâche en validation.

Preuves vertes ce tour : 35/35 tests (11 moteur nature + 17 redirects + 7 hub), `tsc` 0 erreur, `service_role`/`window.print` 0 hit, build compile OK (l'échec `Collecting page data` /admin est pré-existant, prouvé via stash). Rapports : `docs/hub/H0,H3,H4,H5,H6,H7` verts + `H8` NO-GO posé (14 jours de télémétrie requis avant toute suppression, aucune exécutée).

Nouveau : moteur `deriveHubNature` + pill Matériel/Voyage/Rando + sélecteur 3 vignettes branchés dans la coquille, headers `x-hub-redirect-*` sur les 307, télémétrie complète (table + endpoint + hook + KPI admin) en 3 migrations.

Reste manuel à ton retour : `supabase db push` (3 migrations), E2E navigateur + boucle curl 307, puis 14 jours d'observation avant H8. Détail par phase dans `docs/hub/H*_completion_report.md`.
