# H3 — Coquille HubShell + routes /hub + nature sortie — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/hub` ouvert sur l'aventure active (3 natures), chrome 3 colonnes desktop / plein écran mobile, nature sortie branchée par composition (zéro régression `/voyages/*`), socle terrain absorbé (hook + shell renommé).

**Architecture:** Miroir du layout de segment Y (`voyages/[slug]/layout.tsx`) : le layout `/hub` charge l'aventure UNE fois (server helper partagé avec l'API), dérive le profil, monte le `HubShell` client unique. Sortie = liens profonds vers `/voyages/[slug]/…` (jamais re-rendus). Legacy terrain déplacé vers `TerrainShell` (comportement identique, hook extrait), `/terrain` vivant jusqu'en H5.

**Tech Stack:** Next App Router (server layout + client shell), RLS via server client, dynamic imports pourRadar/carte si besoin (aucun en H3).

**Spec:** `docs/CHANTIER_H_HUB_VOYAGEUR.md` §2.1 + §9.2 (H3).

## Global Constraints

- `deriveHubProfile` reste l'unique décideur (le shell ne filtre que l'affichage).
- Aucun littéral `/hub/`, `/voyages/`, segment ou hex non-canonique dans les nouveaux fichiers (H-D85).
- `/terrain`, `/materiel/*`, `/voyages/*`, `/groupes` continuent de fonctionner (aucun redirect avant H5).
- `default adventure` = possession si aucun cookie (jamais de cul-de-sac) ; trip sortie introuvable → repli possession (H-AUTO-15).
- Chiffres H3 : overview = countdown (dates TripLite), alertes, prochaine action, carte pays — calculés, jamais recopiés.

---

### Task 1: hubSectionFromPathname + getHubAdventureData (TDDverktyg)

**Files:**
- Modify: `src/features/hub/registry/hubSectionRegistry.ts` (+ `hubSectionFromPathname(pathname): HubSectionId | null`)
- Create: `src/features/hub/server/getHubAdventureData.ts`
- Modify: `src/app/api/hub/adventures/route.ts` (délègue au helper, même payload)
- Test: `tests/features/hub/hubPathname.spec.ts` (3–5 tests)

**Interfaces :**
```typescript
export function hubSectionFromPathname(pathname: string | null): HubSectionId | null;
// '/hub' -> null, '/hub/kit' -> 'kit', '/hub/inconnu' -> null, '/voyages/x' -> null
export interface HubAdventureData {
  adventure: ActiveAdventureData;
  input: HubAdventureInput;          // compteurs résolus serveur (enabledSections = base serveur)
  trip: TripFull | null;             // nature sortie uniquement
  groupLabel: string | null;         // nature collectif : nom du groupe/équipage
  linkedTripSlug: string | null;     // collectif : slug du voyage lié (CTA)
}
export async function getHubAdventureData(): Promise<HubAdventureData>;
```
**Règles :** cookie via `getActiveAdventure()` ; défaut possession ; sortie → `getTripBySlug(slug, userId)` (import `@/lib/queries-trips`, existant), null → repli possession ; possession → counts (`product_ownership`, `alerts` non résolues, `materiel_loans` statut != 'rendu') ; collectif groupe → `travel_groups`+`group_members` (members, pending invites), voyages liés via `trips` eq `group_id` ; collectif equipage → `fetchUserCrews` puis match id (members, active_trips_count, next_trip?.slug). API route : même helper, payload inchangé `{groups, pendingInvites, crews, possession}`.

- [ ] Step 1: spec pathname RED (fonction inexistante) → GREEN avec implémentation.
- [ ] Helper + refactor API (pas de test réseau : vérifié par tsc + build + recette H8).
- [ ] Commit `feat(h3): hubSectionFromPathname + getHubAdventureData partagé`.

### Task 2: Absorption terrain (hook + rename, zéro régression)

**Files:**
- Create: `src/features/hub/hooks/useHubLiveSensors.ts` (extraction à l'identique : online/offline, battery + ultra-save ≤15%, geolocation watch + compass — repris de HubShell.tsx:22-96)
- Create (move): `src/features/hub/components/TerrainShell.tsx` (contenu actuel de HubShell.tsx, utilise le hook)
- Delete: ancien `HubShell.tsx` (remplacé en Task 4 — pas de doublon)
- Modify: `src/features/hub/index.ts` (export TerrainShell, plus HubShell), `src/app/terrain/page.tsx` (import TerrainShell)
- Test: `npx tsc` + `npm test` verts + `/terrain` monte toujours (build).

- [ ] Extraire le hook (copie exacte, mêmes signatures store) → brancher TerrainShell → maj exports/page → vérifier.
- [ ] Commit `refactor(h3): extraction useHubLiveSensors + TerrainShell (D1, /terrain intact)`.

### Task 3: Briques du shell (sidebars, sheet, network, picker)

**Files (create) :**
- `src/features/hub/components/HubSidebarLeft.tsx` — props `{adventure, profile, counts, onOpenPicker}` : AdventureSwitcher + nav `visibleHubSections(profile)` (lien `hubSectionHref`, icône, label, compteur, actif via pathname) + bouton picker. `<aside>` autorisé (canonique H-D85 R10).
- `src/features/hub/components/HubSidebarRight.tsx` — props `{profile, nature}` : carte Contexte (nature/party/scale) + slots widgets (`hubWidgetsForNature ∩ profile.widgets`, titre + hauteur estimée, repli au-delà de 1800). `<aside>` canonique.
- `src/features/hub/components/HubMobileSectionsSheet.tsx` — miroir TripMobileSectionsSheet (GlassSheet, haptique, compteurs, push href registre).
- `src/features/hub/components/HubNetworkStatus.tsx` — via `useOnlineStatus` (aucun littéral navigator → R8 clean), UN seul indicateur (R5 parcours).
- `src/features/hub/components/HubSectionPicker.tsx` — généralisation TripSectionPicker : props `{adventureKey, profile, serverEnabled, onChange(custom)}`, customs en state initialisé depuis localStorage `lkdv_hub_sections_<key>`, persistance locale, sections cœur par nature (possession: inventaire ; sortie: overview+safety ; collectif: groupe).

- [ ] Implémenter les 5 (patterns Y copiés, `var(--…)` uniquement) → tsc + H-D85 verts.
- [ ] Commit `feat(h3): briques HubShell (sidebars, sheet, network, picker)`.

### Task 4: HubShell + routes /hub + provider root

**Files:**
- Create: `src/features/hub/components/HubShell.tsx` — props `{adventure, profile, baseEnabled, trip, groupLabel, linkedTripSlug, counts, children}` : miroir TripHubShell (AppShellDesktop + MobilePageShell, mémoire `setLastSection(adventureKey, section)`, `applyLKDVStatusBarTheme`, customs → profil effectif via `deriveHubProfile`, picker state). useHubLiveSensors branché (actif = mode live sortie/terrain).
- Create: `src/app/hub/layout.tsx` (server : `getHubAdventureData()` → `deriveHubProfile(input, new Date())` → `<HubShell …>`), `src/app/hub/page.tsx` (overview par nature : possession = compteurs+liens ; sortie = countdown/dates + « Ouvrir le voyage » + liens sections ; collectif = label/membres/invites + « Entrer dans le voyage » si lié), `src/app/hub/[section]/page.tsx` (registre : incompatible → notFound ; sortie → redirect `hubSectionHref` (301? non — 307 temporaire, cohérent H5) ; possession/collectif → page section générique pilotée registre en H3, enrichie H4), `loading.tsx` + `error.tsx` (hub + section).
- Modify: `src/app/layout.tsx` (ActiveAdventureProvider + `getActiveAdventure()`, DANS ActiveTripProvider).
- Modify: `tests/design/h-d85-guard.spec.ts` (burn R1/R2hub : retirer `HubShell.tsx` des exemptions — réécrit propre ; les 9 vues legacy restent jusqu'en H5).

- [ ] Implémenter → G1 G2 G3 → `npm run build` (G4, nouvelles routes) → captures /hub × 3 natures × 2 viewports + inspection HIG → revue Rams (invariance 4 zones) / Nielsen (constance) → MISSION_LOG + commit + tag h3-done + push.

## Self-Review

- §2.1 couvert : layout + page + [section] + loading/error ✅ ; 3 colonnes/GlassSheet ✅ ; registres ✅ ; mémoire section ✅ ; status bar ✅.
- Sortie : composition stricte (liens, pas de re-rendu) → régression zéro ✅ (captures avant/après sans objet : rien n'a changé sous /voyages — H-AUTO-17).
- D1 : hook extrait + shell renommé, /terrain vivant ✅ ; burn-down H-D85 partiel ✅.
- Back Android + offline complet = H6 (tracé, pas oublié).
