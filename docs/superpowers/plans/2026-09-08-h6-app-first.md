# H6 — App-first — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Retour Android hub (section → aperçu → sélecteur → sortie), offline vérifié (SW + Dexie composés), cibles 44px mesurées, safe-areas vérifiées.

**Architecture:** Transition back pure testée + hook Capacitor fin ; SW : precache `/hub` (remplace `/materiel` redirigé) + bump version ; sortie offline et file sync déjà composés (vérification, zéro nouveau code) ; mesure runtime 44px via playwright (preuve H8).

**Tech Stack:** Existants (@capacitor/app, Dexie, SW).

**Spec:** `docs/CHANTIER_H_HUB_VOYAGEUR.md` §9.2 (H6).

## Global Constraints

- Back : ne quitte JAMAIS l'app depuis le hub (miroir Y5.4) ; web non-natif = no-op.
- SW : bump `lkdv-v2` → `lkdv-v3` (purge auto) ; precache `/hub` (pas les sections — runtime cache).
- H-D85 : fichiers hub en `var(--…)` ; le hook n'écrit aucune URL en littéral (hubSectionHref + `/hub` racine autorisée R13 — pas de segment).
- android-development (Kotlin) écartée : chantier = web Capacitor (H-AUTO-33).

---

### Task 1: Retour Android hub (TDD)

**Files:**
- Create: `src/features/hub/hooks/useAndroidHubBackNav.ts` — `hubBackTransition(activeSection: HubSectionId | null, switcherOpen: boolean): 'to-overview' | 'open-switcher' | 'close-switcher'` (pure) + hook `useAndroidHubBackNav(activeSection)` (Capacitor, dialogue via events `hub:open-switcher` / `hub:close-switcher` / état `hub:switcher-state`).
- Test: `tests/features/hub/hubBackNav.spec.ts` (~6 tests : section→overview, overview fermé→open, ouvert→close, null→open, déterminisme).
- Modify: `AdventureSwitcher` (prop `forceOpenSignal?: number` + émission `hub:switcher-state` + écoute `hub:close-switcher`), `HubShell` (écoute `hub:open-switcher` → signal++, suit `hub:switcher-state`, monte le hook).

**Règles (exact) :** section non-nulle ET non-… (toute section, y compris overview ? overview = racine → open-switcher) : `activeSection !== null` → 'to-overview' (`router.push('/hub')`) ; null/overview + fermé → 'open-switcher' ; ouvert → 'close-switcher'. Note : `hubSectionFromPathname('/hub')` = null = aperçu ✅ (pas de cas overview-segment sauf sortie-overview qui redirecte).

- [ ] Spec RED → impl GREEN → câblage → tsc → commit `feat(h6): retour Android hub (section→aperçu→sélecteur)`.

### Task 2: SW offline hub

**Files:** `public/sw.js` — `CACHE_VERSION lkdv-v2→lkdv-v3`, PRECACHE `/materiel`→`/hub`.
- [ ] Éditer → vérifier (grep) → commit `fix(h6): SW precache /hub (remplace /materiel redirigé)`.

### Task 3: Mesures runtime (preuves)

- [ ] Script playwright (projet, supprimé après) : `/hub`, `/hub/kit`, `/hub/groupe` (cookie collectif) mobile 430×932 → % boutons/liens ≥44px (géométrie `getBoundingClientRect`, éléments visibles). Consigner chiffres (pas de fix aveugle : si <95%, corriger puis remesurer).
- [ ] Safe-areas : grep `safe-area-inset` dans shells hub + inspection captures (insets visibles) → verdict.
- [ ] Offline : inventaire sections = SW runtime cache (visité→offline OK) ; sortie = OfflineToggleWidget composé (TripSidebarRight) + tripSyncDB ; indicateur unique HubNetworkStatus. Zéro code — vérification + traçage.

### Task 4: Portes + tag

- [ ] G1 0 · G2 ≥1153 · G3 · captures · MISSION_LOG + commit `chore(h6): mesures + clôture` + tag h6-done + push.

## Self-Review

- §H6 couvert : safe-areas ✅ (vérifié), 44px ✅ (mesuré), back Android ✅ (hook étendu), offline ✅ (composé+vérifié, SW corrigé).
- Kat Holmes (inclusion) : revue à la mesure 44px — objections traitées.
